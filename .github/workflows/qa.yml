name: QA

on:
  push:
  pull_request:

jobs:
  sonar:
    uses: autumn-library/workflows/.github/workflows/sonar.yml@main
    with:
      github_repository: nixel2007/moskito
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
